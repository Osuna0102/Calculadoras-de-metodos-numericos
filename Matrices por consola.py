import numpy as np
import os

cantidad = (input("Indique número de matrices a ingresar: "))

if cantidad == '2':

    A=np.array([[float(z) for z in x.split()] for x in input("Ingrese las filas separados por ; y las columnas por espacios: ").split(";")])
    B=np.array([[float(z) for z in x.split()] for x in input("Ingrese las filas separados por ; y las columnas por espacios: ").split(";")])

    
if cantidad == '3':
    
    A=np.array([[float(z) for z in x.split()] for x in input("Ingrese las filas separados por ; y las columnas por espacios: ").split(";")])
    B=np.array([[float(z) for z in x.split()] for x in input("Ingrese las filas separados por ; y las columnas por espacios: ").split(";")])
    C=np.array([[float(z) for z in x.split()] for x in input("Ingrese las filas separados por ; y las columnas por espacios: ").split(";")])


if cantidad == '4':
    
    A=np.array([[float(z) for z in x.split()] for x in input("Ingrese las filas separados por ; y las columnas por espacios: ").split(";")])
    B=np.array([[float(z) for z in x.split()] for x in input("Ingrese las filas separados por ; y las columnas por espacios: ").split(";")])
    C=np.array([[float(z) for z in x.split()] for x in input("Ingrese las filas separados por ; y las columnas por espacios: ").split(";")])
    D=np.array([[float(z) for z in x.split()] for x in input("Ingrese las filas separados por ; y las columnas por espacios: ").split(";")])


if cantidad == '5':
    
    A=np.array([[float(z) for z in x.split()] for x in input("Ingrese las filas separados por ; y las columnas por espacios: ").split(";")])
    B=np.array([[float(z) for z in x.split()] for x in input("Ingrese las filas separados por ; y las columnas por espacios: ").split(";")])
    C=np.array([[float(z) for z in x.split()] for x in input("Ingrese las filas separados por ; y las columnas por espacios: ").split(";")])
    D=np.array([[float(z) for z in x.split()] for x in input("Ingrese las filas separados por ; y las columnas por espacios: ").split(";")])
    F=np.array([[float(z) for z in x.split()] for x in input("Ingrese las filas separados por ; y las columnas por espacios: ").split(";")])


if cantidad == '6':
    
    A=np.array([[float(z) for z in x.split()] for x in input("Ingrese las filas separados por ; y las columnas por espacios: ").split(";")])
    B=np.array([[float(z) for z in x.split()] for x in input("Ingrese las filas separados por ; y las columnas por espacios: ").split(";")])
    C=np.array([[float(z) for z in x.split()] for x in input("Ingrese las filas separados por ; y las columnas por espacios: ").split(";")])
    D=np.array([[float(z) for z in x.split()] for x in input("Ingrese las filas separados por ; y las columnas por espacios: ").split(";")])
    F=np.array([[float(z) for z in x.split()] for x in input("Ingrese las filas separados por ; y las columnas por espacios: ").split(";")])
    E=np.array([[float(z) for z in x.split()] for x in input("Ingrese las filas separados por ; y las columnas por espacios: ").split(";")])
 

ecuacion = input("Ingrese la ecuacion a operar:  ")
print(eval(ecuacion))

















    
    
